extends ../layout
block content
    .content
        .container
            h1  Portfoliu
            if(categories)
                ol.portfolio
                    each category in categories
                        include category_block
