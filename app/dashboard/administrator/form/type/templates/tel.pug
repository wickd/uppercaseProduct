.input-group(style="width: 300px")
    //span.input-group-addon !{element.getCountryCode()}
    input(data-mask=h.isset(element.getMask()) ? element.getMask() : '', type="tel", name=element.getName(), value=element.getValue())&attributes(element.attributes)